<h1>Products#edit</h1>
<p>Find me in app/views/products/edit.html.erb</p>
    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h1 class="card-title">Enter Location</h1>
          <%= form_with(model: @products, local: true) do |form| %>
            <% if @products.errors.any? %>
              <div id="error_explanation">
                <h2><%= pluralize(@products.errors.count, "error") %> prohibited this package from being saved:</h2>
                <ul>
                  <% @products.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                  <% end %>
                </ul>
              </div>
            <% end %>

            <%= form.hidden_field :user_id, value: params[:package_id] %>
            <%= form.hidden_field :cart_id, value: params[:package_id] %>


            <div class="actions">
              <%= form.submit "Create Package", class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>